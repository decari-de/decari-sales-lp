---
import ArticleLayout from "../../layouts/ArticleLayout.astro";
import articleContent from "../../content/ratgeber-warum-mehr-klienten-nicht-die-loesung-ist.html?raw";

const cleanContent = articleContent
    .replace(/<nav class="text-sm text-gray-500 mb-6">[\s\S]*?<\/nav>/, "")
    .replace(/<header class="mb-10">[\s\S]*?<\/header>/, "")
    .replace(/<div class="mt-16 bg-teal[\s\S]*?<\/div>/, "")
    .replace(/<div class="mt-12 pt-8[\s\S]*?<\/div>/, "");
---

<ArticleLayout
    title="Warum mehr Klienten nicht die Lösung ist"
    description="Der Paradigmenwechsel: Wie Pflegedienste mit bestehenden Klienten mehr verdienen können – ohne neues Personal einzustellen."
    category="Strategie"
    breadcrumbTitle="Warum mehr Klienten nicht die Lösung ist"
    slug="warum-mehr-klienten-nicht-die-loesung-ist"
    publishedDate="2025-01-15"
>
    <Fragment set:html={cleanContent} />
</ArticleLayout>
