---
import BlogArticleLayout from "../../layouts/BlogArticleLayout.astro";
import articleContent from "../../content/blog-pflegegeld-sachleistung.html?raw";

// Remove header, breadcrumb, and CTA from the extracted content
const cleanContent = articleContent
    .replace(/<nav class="text-sm text-gray-500 mb-6">[\s\S]*?<\/nav>/, "")
    .replace(/<header class="mb-10">[\s\S]*?<\/header>/, "")
    .replace(/<div class="mt-16 bg-teal[\s\S]*?<\/div>/, "")
    .replace(/<div class="mt-12 pt-8[\s\S]*?<\/div>/, "");
---

<BlogArticleLayout
    title="Pflegegeld zur Sachleistung: So steigern Pflegedienste ihren Umsatz pro Patient"
    description="Warum viele Patienten auf Pflegegeld als Einkommen angewiesen sind – und wie vorrangige Sozialleistungen das Problem lösen. Mit konkreten Zahlen und Rechenbeispielen."
    category="Finanzierung"
    breadcrumbTitle="Pflegegeld zur Sachleistung"
    slug="pflegegeld-sachleistung"
    publishedDate="2026-02-18"
>
    <Fragment set:html={cleanContent} />
</BlogArticleLayout>
