---
import BlogArticleLayout from "../../layouts/BlogArticleLayout.astro";
import articleContent from "../../content/blog-angebotskuerzung.html?raw";

const cleanContent = articleContent
    .replace(/<nav class="text-sm text-gray-500 mb-6">[\s\S]*?<\/nav>/, "")
    .replace(/<header class="mb-10">[\s\S]*?<\/header>/, "")
    .replace(/<div class="mt-16 bg-teal[\s\S]*?<\/div>/, "")
    .replace(/<div class="mt-12 pt-8[\s\S]*?<\/div>/, "");
---

<BlogArticleLayout
    title="Warum Pflegeangebote gekürzt werden – und was Ihr Pflegedienst dagegen tun kann"
    description="55,9 % aller Pflegebedürftigen wählen reines Pflegegeld, nur 3,3 % reine Sachleistung. Warum Klienten Angebote kürzen und wie Sie als Pflegedienst die Finanzierungsbrücke bauen."
    category="Strategie"
    breadcrumbTitle="Angebotskürzung"
    slug="angebotskuerzung"
    publishedDate="2026-02-18"
>
    <Fragment set:html={cleanContent} />
</BlogArticleLayout>
